This is a terminal-based to-do list app built with Python.
It allows the user to add, view, and delete tasks.
Tasks are automatically saved to a tasks.json file so they persist between sessions.

Run app.py in VS Code or type "python app.py" in the terminal.
